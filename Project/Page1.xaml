<Page
    x:Class="Project.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>

            <CalendarView Grid.Row="0" x:Name="Calendar" HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="450" SelectedDatesChanged="SelectedDatesChanged" CalendarIdentifier="GregorianCalendar" FontSize="21.333" Margin="52,68,0,148" />
            <AppBar Grid.Row="1" VerticalAlignment="Bottom"></AppBar>
        </Grid>

    </Grid>
</Page>
